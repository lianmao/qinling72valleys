# 高德开放平台 —— 复制为 .env.local 后填入真实值（.env.local 已被 .gitignore 忽略）
#
# 取值：高德控制台 → 应用管理 → 我的应用 → 创建应用 → 添加 Key
#       「服务平台」必须选 Web端(JS API)
#       安全密钥：同一个 Key 详情页里的「安全密钥 JSCode」（JS API v2.0 必需）
#
# ★ 白名单：Key 的「域名白名单」需包含
#     lianmao.github.io
#   （本地开发若被白名单拦住，临时加 localhost:5173 / 127.0.0.1:8123，或开发期先留空）
VITE_AMAP_KEY=
VITE_AMAP_SECURITY=
